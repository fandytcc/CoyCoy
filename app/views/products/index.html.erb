<div class="row">
  <% @products.each do |product| %>
    <div class="col-md-4">
      <div class="panel panel-default">
        <div class="panel-heading">
          <div class="panel-title"><%= product.name %></div>
        </div>
        <div class="panel-body">
          <p><%= product.description %></p>
        </div>
        <div class="panel-footer">$ <%= product.price %></div>
        <%= link_to "Add", list_items_path(cart: current_user.cart.id, product: product), method: :post %>
      </div>
    </div>
  <% end %>
</div>
